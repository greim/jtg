var Transform = require('./transform')

var function(opts, gen) {
  if (typeof opts === 'function') {
    gen = opts, opts = {};
  }
};

/*

pony(function*(input, output){

  // handle 'data' from upstream
  var chunk = yield input()

  // emit 'data' to downstream
  yield output('stuff');

  // emit 'error'
  throw(new Error('oops'));

  // delay for 20 ms
  yield this.delay(20);

  // emit 'end'
})

*/










